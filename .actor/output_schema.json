{
  "actorOutputSchemaVersion": 1,
  "title": "YouTube Transcriber Output",
  "properties": {
    "success": {
      "title": "Success",
      "type": "boolean",
      "template": "{{success}}"
    },
    "youtubeUrl": {
      "title": "YouTube URL",
      "type": "string",
      "template": "{{youtubeUrl}}"
    },
    "videoId": {
      "title": "Video ID",
      "type": "string",
      "template": "{{videoId}}"
    },
    "transcript": {
      "title": "Transcript",
      "type": "string",
      "template": "{{transcript}}"
    },
    "provider": {
      "title": "Provider",
      "type": "string",
      "enum": ["youtube-transcript", "openrouter"],
      "template": "{{provider}}"
    },
    "language": {
      "title": "Language",
      "type": "string",
      "template": "{{language}}"
    },
    "usage": {
      "title": "Usage",
      "type": "object",
      "template": "{{usage}}"
    },
    "generatedAt": {
      "title": "Generated At",
      "type": "string",
      "template": "{{generatedAt}}"
    },
    "error": {
      "title": "Error",
      "type": "string",
      "template": "{{error}}"
    },
    "details": {
      "title": "Details",
      "type": "string",
      "template": "{{details}}"
    }
  },
  "required": ["success", "youtubeUrl", "generatedAt"]
}
